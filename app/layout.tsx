import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Gritliy - Engineering Excellence, One Visionary Hire at a Time",
  description: "Specialized recruiting and talent acquisition connecting world-class engineering teams with extraordinary opportunities.",
  keywords: "technical recruiting, engineering talent, AI recruiting, talent acquisition",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>{children}</body>
    </html>
  );
}